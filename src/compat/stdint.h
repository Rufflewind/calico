/*@self.public()*/
/* This compatibility header exists only for pedantic reasons.  Since
   <stdint.h> is really useful but tricky to emulate, we just assume it exists
   on sensible systems (even MSVC has it despite not supporting C99) */
#include <stdint.h>
